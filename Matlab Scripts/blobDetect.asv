function [] = blobDetect(image)

   %h = fspecial('log');
   %image = imfilter(image, h);
   for hsize = 4:10
       for sigma = 0.5:0.5:2
       
           h = fspecial('log');
           image = imfilter(image, h);
       end
   end
   
   figure
   imshow(image);
end
